# 开发与验证记录（V1.0.0）

- 日期：2026-04-22
- 执行人：Codex
- 基线文档：`访达右键增强工具设计文档_V1.0.0`、`Codex_执行说明_V1.0.0.txt`

## 一、本次开发范围

本次按照 `设计文档/v1.0` 的 MVP 约束完成了以下实现：

- 建立 `App / Extension / Shared / Tests` 模块化工程结构（Swift Package 形式）。
- 建立共享配置模型（菜单项、显示场景、动作类型、应用设置、外部应用路径）。
- 实现 App Group 风格配置存储接口（`UserDefaults` 存储实现 + 内存实现用于测试）。
- 实现菜单管理页核心逻辑（菜单开关、显示场景配置、顺序调整、应用设置编辑）。
- 实现扩展侧菜单构建器（按场景过滤、按启用状态过滤、按“隐藏未安装应用”过滤）。
- 实现动作分发：
  - 新建文件夹（冲突自动递增）
  - 新建 txt / md / json（冲突自动递增，模板内容写入）
  - 在终端中打开 / 在 iTerm 中打开 / 用 VS Code 打开 / 用 Cursor 打开
  - 复制路径

## 二、代码文件新增

### 工程
- `Package.swift`

### Shared
- `Shared/Constants/SharedConstants.swift`
- `Shared/Models/RightClickScene.swift`
- `Shared/Models/MenuGroup.swift`
- `Shared/Models/MenuActionType.swift`
- `Shared/Models/ExternalApplication.swift`
- `Shared/Models/SceneVisibility.swift`
- `Shared/Models/MenuItemConfiguration.swift`
- `Shared/Models/AppSettings.swift`
- `Shared/Models/SharedConfiguration.swift`
- `Shared/Storage/ConfigurationStore.swift`
- `Shared/Storage/UserDefaultsConfigurationStore.swift`
- `Shared/Storage/InMemoryConfigurationStore.swift`
- `Shared/Utilities/FileNameConflictResolver.swift`
- `Shared/Utilities/SelectionSceneResolver.swift`
- `Shared/Actions/MenuActionDescriptor.swift`

### Extension
- `Extension/FinderSync/FinderSelectionContext.swift`
- `Extension/MenuBuilder/MenuDisplayItem.swift`
- `Extension/MenuBuilder/MenuBuilder.swift`
- `Extension/ActionDispatcher/Dependencies.swift`
- `Extension/ActionDispatcher/ActionDispatcher.swift`

### App
- `App/ViewModels/MenuManagementViewModel.swift`
- `App/Views/MenuManagementView.swift`
- `App/Views/MenuItemDetailView.swift`
- `App/Settings/ApplicationSettingsView.swift`
- `App/AppEntry/RootConfigurationView.swift`

### Tests
- `Tests/UnitTests/TestHelpers/TemporaryDirectory.swift`
- `Tests/UnitTests/MenuBuilderTests.swift`
- `Tests/UnitTests/ActionDispatcherTests.swift`
- `Tests/UnitTests/UserDefaultsConfigurationStoreTests.swift`
- `Tests/UnitTests/MenuManagementViewModelTests.swift`

## 三、本次新增/修改测试

- `MenuBuilderTests`
  - 空白处右键过滤
  - 文件右键过滤
  - 文件夹右键过滤
- `ActionDispatcherTests`
  - 新建 Markdown 文件与命名冲突
  - 新建文件夹命名冲突
  - 复制路径
  - 终端打开命令参数
- `UserDefaultsConfigurationStoreTests`
  - 默认配置加载
  - 配置持久化读写
- `MenuManagementViewModelTests`
  - 菜单项开关与场景更新
  - 排序与顺序重排

## 四、已执行测试命令与结果

执行命令：

```bash
swift test
```

结果：

- 全部通过（11 个测试用例，0 失败）
- 最近一次执行时间：2026-04-22 10:37（本地时区）

## 五、回归用例 1-7 影响评估

- 用例 1（空白处右键过滤）：已覆盖，受影响且通过自动化验证。
- 用例 2（文件右键菜单过滤）：已覆盖，受影响且通过自动化验证。
- 用例 3（文件夹右键打开终端）：已覆盖，受影响且通过自动化验证。
- 用例 4（新建 Markdown 文件）：已覆盖，受影响且通过自动化验证。
- 用例 5（命名冲突处理）：已覆盖，受影响且通过自动化验证。
- 用例 6（配置变更即时生效）：受影响，已通过 ViewModel + MenuBuilder 链路验证（建议后续补 Finder 集成手工回归）。
- 用例 7（未安装应用隐藏）：已覆盖“隐藏未安装应用菜单”的过滤逻辑（建议后续补 Finder 集成手工回归）。

## 六、版本与变更建议

- 建议版本号：`V1.0.0`（首个可运行 MVP 基线）。
- 是否需要更新 `CHANGELOG`：是。建议在根目录新增 `CHANGELOG.md` 或基于模板落地首个发布记录。
