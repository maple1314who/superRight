# 开发验证记录 V3.10.0

日期：2026-06-09

## 目标

调整主配置界面顶部布局，让表格页和旧版“超级右键”一样保留统一顶部留白，避免内容贴近窗口上沿。

## 已完成

- 新增 `RootConfigurationLayout.contentTopPadding`，统一管理配置页顶部留白。
- “新建文件”“发送文件到...”“常用目录”“文件(夹)图标”“工具箱”五个表格页顶部留白统一调整为 `90pt`。
- 保留 full-size content view 和左侧栏顶部系统窗口按钮布局。
- 主 App 与 Finder Extension 版本推进到 `3.10.0 / 2026060918`。

## 影响文件

- `App/AppEntry/RootConfigurationView.swift`
- `右键增强.xcodeproj/project.pbxproj`

## 回归测试

- `swift test`：51 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。

## 手动验证建议

1. 打开主 App，确认表格顶部留白和旧版截图一致，内容不贴窗口上沿。
2. 切换五个表格页，确认顶部间距一致。
3. 确认红黄绿窗口按钮仍位于左侧栏顶部，窗口可正常拖动。

## 已知限制

- `groupMenuByCategory` 开关仍未控制 FinderSync 的分组/扁平切换。
- `launchAtLogin` 仍只有配置字段，尚未接入 Login Item。
- Developer ID 签名与公证仍受证书和 `notarytool` 凭据限制。
