# 开发验证记录 V3.3.0

- 日期：2026-06-09
- 执行人：Codex
- 版本目标：统一应用展示名称、真实版本管理，并补齐核心模块注释。

## 1. 本次完成范围

### 1.1 应用名称
- 应用内展示名称统一为“右键增强”。
- 移除 UI 中旧名称“超级右键”和旧硬编码版本号。
- 主 App `Info.plist` 生成配置新增：
  - `CFBundleDisplayName = 右键增强`
  - `CFBundleName = 右键增强`

### 1.2 版本管理
- 配置界面侧边栏版本号改为读取 `CFBundleShortVersionString`。
- 主 App 与 Finder Extension 的版本统一为：
  - `MARKETING_VERSION = 3.3.0`
  - `CURRENT_PROJECT_VERSION = 2026060903`
- 新增版本管理说明文档，后续发版必须同步工程版本、CHANGELOG 和验证记录。

### 1.3 文档
- 补充核心源码注释，覆盖：
  - `Shared`：共享配置、全局设置、模板、目标目录、IPC 请求模型。
  - `ExtensionCore`：菜单构建、动作分发。
  - `AppCore`：配置根视图、配置 ViewModel。
  - `Finder Extension`：右键菜单入口、点击转发、监听目录。
  - `主 App`：生命周期、Dock/菜单栏隐藏、权限检查、请求队列。
- 新增模块说明文档，记录当前工程分层、关键文件和功能接入规则。
- 更新 `CHANGELOG.md`，新增 V3.3.0 条目。
- 新增 `Codex工作流Skill_V3.3.0.md`，记录后续开发必须遵守的 Skill 工作流。

## 2. 回归测试

### 2.1 Swift Package 单元测试
执行目录：
`/Users/maple/Documents/Project/Swift/SuperRight`

命令：
```bash
swift test
```

结果：
- 31 tests
- 0 failures

### 2.2 Xcode 主工程构建
执行目录：
`/Users/maple/Documents/Project/Swift/右键增强`

命令：
```bash
xcodebuild -project '右键增强.xcodeproj' -scheme '右键增强' -configuration Debug -destination 'platform=macOS' build
```

结果：
- `BUILD SUCCEEDED`

### 2.3 生成包版本校验
主 App：
- `CFBundleDisplayName = 右键增强`
- `CFBundleName = 右键增强`
- `CFBundleShortVersionString = 3.3.0`
- `CFBundleVersion = 2026060903`

Finder Extension：
- `CFBundleDisplayName = RightClickFinderExtension`
- `CFBundleName = RightClickFinderExtension`
- `CFBundleShortVersionString = 3.3.0`
- `CFBundleVersion = 2026060903`

## 3. 手工验证建议

1. 启动 `/Applications/右键增强.app` 或 Debug 构建产物。
2. 确认侧边栏显示“右键增强 3.3.0”。
3. 确认系统 Dock、菜单栏和应用切换器中不再出现“超级右键”。
