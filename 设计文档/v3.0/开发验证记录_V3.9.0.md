# 开发验证记录 V3.9.0

- 日期：2026-06-09
- 执行人：Codex
- 版本目标：集成 Sparkle 自动更新，生成可发布到 GitHub Release 的更新包和 Appcast。
- 版本号：`3.9.0 / 2026060909`

## 1. 本次完成范围

### 1.1 Sparkle 接入
- 主 App 通过 Swift Package 集成 Sparkle `2.9.3`。
- `AppDelegate` 持有 `SPUStandardUpdaterController`，启动后自动检查更新。
- 菜单栏增加“检查更新…”入口，手动触发 Sparkle 更新检查。

### 1.2 更新源与签名
- `SUFeedURL` 指向 GitHub Release 资产：`https://github.com/maple1314who/superRight/releases/latest/download/appcast.xml`。
- `SUPublicEDKey` 配置为 `k6OI45dm0EFPBe6BvbzV3lrPh2yl0fqcDbziaI6WRIU=`。
- 私钥由 Sparkle 工具写入本机 Keychain，不提交到 Git。

### 1.3 沙盒权限
- 主 App entitlements 增加 `com.apple.security.network.client`。
- 主 App entitlements 增加 Sparkle installer/downloader 所需 mach lookup 例外：
  - `$(PRODUCT_BUNDLE_IDENTIFIER)-spks`
  - `$(PRODUCT_BUNDLE_IDENTIFIER)-spki`

## 2. 影响文件

### Xcode 主工程
- `右键增强.xcodeproj/project.pbxproj`
- `右键增强.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`
- `右键增强/AppDelegate.swift`
- `右键增强/右键增强.entitlements`
- `右键增强/右键增强.debug.entitlements`
- `.gitignore`

### Swift Package
- `App/AppEntry/RootConfigurationView.swift`
- `CHANGELOG.md`
- `设计文档/v3.0/开发验证记录_V3.9.0.md`
- `设计文档/v3.0/模块说明_V3.9.0.md`
- `设计文档/v3.0/版本管理说明_V3.9.0.md`

## 3. 回归测试

### 3.1 Swift Package 单元测试
执行目录：
`/Users/maple/Documents/Project/Swift/SuperRight`

命令：
```bash
swift test
```

结果：
- 42 tests
- 0 failures

### 3.2 Xcode 主工程构建
执行目录：
`/Users/maple/Documents/Project/Swift/右键增强`

命令：
```bash
xcodebuild -project '右键增强.xcodeproj' -scheme '右键增强' -configuration Debug -destination 'platform=macOS' build
```

当前结果：
- `BUILD SUCCEEDED`

### 3.3 Release 包与 Appcast
执行命令：
```bash
xcodebuild -project '右键增强.xcodeproj' -scheme '右键增强' -configuration Release -destination 'platform=macOS' -derivedDataPath build/DerivedData build
hdiutil create -volname '右键增强 3.9.0' -srcfolder 'build/DerivedData/Build/Products/Release/右键增强.app' -ov -format UDZO 'dist/sparkle/右键增强-3.9.0.dmg'
generate_appcast --download-url-prefix 'https://github.com/maple1314who/superRight/releases/latest/download/' dist/sparkle
```

结果：
- Release 构建：`BUILD SUCCEEDED`
- DMG：`dist/sparkle/右键增强-3.9.0.dmg`
- Appcast：`dist/sparkle/appcast.xml`
- Appcast 已包含 `sparkle:edSignature`。
- 产物版本校验：
  - `CFBundleShortVersionString = 3.9.0`
  - `CFBundleVersion = 2026060909`
  - `SUFeedURL = https://github.com/maple1314who/superRight/releases/latest/download/appcast.xml`
  - `SUPublicEDKey = k6OI45dm0EFPBe6BvbzV3lrPh2yl0fqcDbziaI6WRIU=`

### 3.4 GitHub Release 上传
本机状态：
- `gh`：未安装。
- `GITHUB_TOKEN` / `GH_TOKEN`：未配置。
- `curl -I -L https://github.com/maple1314who/superRight/releases/latest/download/appcast.xml`：当前返回 `HTTP/2 404`。
- 已推送 `SuperRight` 文档提交到 `git@github.com:maple1314who/superRight.git`。
- 已在本地生成 Release 资产，但无法在当前环境自动创建 GitHub Release 或上传资产。

后续需要上传到 GitHub Release 的文件：
- `/Users/maple/Documents/Project/Swift/右键增强/dist/sparkle/右键增强-3.9.0.dmg`
- `/Users/maple/Documents/Project/Swift/右键增强/dist/sparkle/appcast.xml`

手工或补齐 Token 后应创建 `v3.9.0` Release，并上传上述两个资产；否则当前 `SUFeedURL` 无法提供 Sparkle 更新。

## 4. 手工验证建议

1. 启动 `右键增强.app`。
2. 确认菜单栏未隐藏时存在“检查更新…”。
3. 点击“检查更新…”，确认 Sparkle 能读取 GitHub Release 的 `appcast.xml`。
4. 发布新版本后，确认旧版本能检测到更新并下载 DMG。
5. 更新安装后检查 Finder Extension 仍可加载并展示右键菜单。

## 5. 已知限制

- Sparkle 私钥只存在本机 Keychain；换机器发版需要迁移或重新生成密钥并同步公钥。
- GitHub Release 发布依赖 `gh`/GitHub Token 或手工上传资产；当前环境未完成资产上传。
- 当前 Release 使用 Apple Development 证书签名，未做 Developer ID 公证；外部分发前应补 Developer ID 签名与 notarization。

后续状态：
- 2026-06-09 复查本机证书，仅有 Apple Development 和 Apple Distribution，未发现 `Developer ID Application`。
- `xcrun notarytool history` 返回缺少凭据，当前环境仍不能完成 Developer ID 公证。

## 6. 剩余任务

- 工具箱扩展项：剪切、iShot、ICNS 转换。
- 文件/文件夹图标支持导入本地图片和拖拽排序。
- 右键菜单分组/子菜单语义。
- 打包、安装、Finder Extension 重启验证。
