# V4.0.0 架构重构说明

日期：2026-06-09

## 目标

把 Finder Extension 侧菜单执行链从集中式 `switch` 重构为可扩展的 V4 架构。后续新增菜单、系统版本差异实现、日志/埋点/重试能力时，不再继续扩大单个执行器文件。

## 设计模式落地

### 策略模式

- 协议：`MenuActionStrategy`
- 入口：`ActionDispatcher.execute(...)`
- 已拆分策略：
  - `CreateFolderActionStrategy`
  - `CreateFileActionStrategy`
  - `OpenExternalApplicationActionStrategy`
  - `CopyPathActionStrategy`
  - `TransferItemsActionStrategy`
  - `OpenDirectoryActionStrategy`
  - `ForwardedOnlyActionStrategy`

每个策略只处理一类动作流程，不直接创建 `Process`、写剪贴板或判断系统版本。

### 工厂模式

- 类型：`MenuActionStrategyFactory`
- 职责：维护 `MenuActionType -> MenuActionStrategy` 映射。
- 新增菜单动作时优先新增策略，然后在 `defaultStrategies()` 注册。

这样执行入口不用再写大 `switch`，菜单动作扩展点集中、可测试。

### 适配器模式

- 文件系统适配器：`FileSystemActionAdapter`
- 外部 App 适配器：`ExternalApplicationActionAdapter`
- 系统版本提供者：`SystemVersionProviding`

适配器隔离 macOS API 差异。当前外部 App 打开仍统一使用 `/usr/bin/open`，但已经把系统版本注入到适配器中；如果后续某些 macOS 版本需要切换 `NSWorkspace`、LaunchServices 或 App 专用 URL Scheme，只需要替换适配器实现，不需要改策略。

### 观察者模式

- 协议：`ActionExecutionObserving`
- 默认实现：`NSLogActionExecutionObserver`
- 组合实现：`CompositeActionExecutionObserver`

观察者记录动作开始、成功和失败。后续可以接入调试窗口、文件日志、统计埋点或失败重试，不污染策略代码。

### 责任链模式

- 基类：`ActionExecutionPreflightLink`
- 默认链：`ActionExecutionPreflightLink.defaultChain()`
- 已接入节点：
  - `DefaultFileNamePreflightLink`
  - `DestinationPathPreflightLink`
  - `SelectedItemsPreflightLink`

责任链在进入具体策略前执行，用于统一校验动作前置条件。顺序是有业务含义的：先检查配置完整性，再检查 Finder 运行时输入，保证错误优先指向根因。

### 空对象模式

- 类型：`ForwardedOnlyActionStrategy`

Finder Extension 不能安全直接执行的动作统一映射到空对象策略，策略返回 `nil` 并记录日志，由 `FinderSync` 的转发链交给主 App 执行。这避免了执行入口出现大量“什么都不做但必须存在”的特殊分支。

## 模块边界

- `ActionDispatcher`：执行门面，只负责组装上下文、触发观察者、调用工厂选择策略。
- `ActionExecutionRequestContext`：策略只读上下文，封装菜单项、Finder 选择和配置。
- `ActionExecutionAdapters`：策略依赖的能力集合。
- `MenuActionStrategyFactory`：策略注册和选择中心。
- `ActionAdapters`：平台和系统 API 隔离层。
- `ActionExecutionPreflightChain`：策略执行前的责任链校验层。

## 影响文件

- `Extension/ActionDispatcher/ActionDispatcher.swift`
- `Extension/ActionDispatcher/ActionExecutionContext.swift`
- `Extension/ActionDispatcher/ActionExecutionObserver.swift`
- `Extension/ActionDispatcher/ActionExecutionPreflightChain.swift`
- `Extension/ActionDispatcher/ActionAdapters.swift`
- `Extension/ActionDispatcher/MenuActionStrategies.swift`
- `Extension/ActionDispatcher/MenuActionStrategyFactory.swift`
- `Extension/ActionDispatcher/Dependencies.swift`
- `CHANGELOG.md`
- `右键增强.xcodeproj/project.pbxproj`

## 回归测试

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。

## 下一步重构方向

1. 主 App `AppExecutionRequestHandler` 已在 V4.0.2 接入策略、工厂、责任链和观察者，在 V4.0.3 抽出应用解析适配器，并在 V4.0.4 抽出文件系统适配器；后续可以继续把删除确认、快捷方式等具体业务拆到独立适配器文件。
2. Finder 菜单构建链可以继续拆成过滤策略、分组策略和展示适配器。
3. Sparkle 更新、权限检测、文件图标写入可以纳入 V4 事件观察者，统一记录用户可诊断日志。

## V4.0.2 主 App 执行链补充

### 新增模式

- 策略模式：`AppExecutionStrategy` 按动作类别执行主 App 请求。
- 工厂模式：`AppExecutionStrategyFactory` 集中维护 `AppExecutionAction -> Strategy` 映射。
- 适配器上下文：`AppExecutionStrategyContext` 把既有 `AppExecutionRequestHandler` 业务函数适配给策略层，避免一次性改动底层行为。
- 责任链模式：`AppExecutionPreflightLink` 在进入策略前校验目标路径、目标目录和复制/移动源文件。
- 观察者模式：`AppExecutionObserving` 记录主 App 执行动作的开始、成功和失败。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionStrategy.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionPreflightChain.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionObserver.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。

## V4.0.20 主 App 新建动作适配器补充

### 新增适配器

- 类型：`AppExecutionCreationAdapter`
- 职责：统一处理主 App 新建文件夹和新建文件动作，包括冲突命名、模板二进制/文本写入、创建后提示音和自动打开。
- 边界：只负责新建动作本身，不负责请求队列、策略选择、Finder Extension 菜单构建或按文件名创建同名文件夹动作。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionCreationAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.20 / 2026060944`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.21 主 App 应用打开执行适配器补充

### 新增适配器

- 类型：`AppExecutionApplicationOpeningAdapter`
- 职责：统一执行固定外部应用和工具箱应用打开目标路径的 `NSWorkspace` 调用，并同步等待打开结果。
- 边界：只负责打开执行，不负责 Bundle ID、默认安装路径、用户配置路径或工具箱别名解析；这些解析规则仍由 `AppExecutionApplicationAdapter` 承担。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionApplicationOpeningAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.21 / 2026060945`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.22 主 App 目录打开适配器补充

### 新增适配器

- 类型：`AppExecutionDirectoryOpeningAdapter`
- 职责：统一处理常用目录动作的目标路径存在性校验、目录类型校验和系统打开调用。
- 边界：只负责打开目录本身，不负责菜单配置、常用目录 UI、请求策略选择或 Finder Extension 转发。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionDirectoryOpeningAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.22 / 2026060946`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.23 主 App 文件传输适配器补充

### 新增适配器

- 类型：`AppExecutionTransferAdapter`
- 职责：统一处理“发送文件到/移动文件到”的请求路径规范化去重、文件系统传输调用、逐项日志和完成提示音。
- 边界：只负责任务级业务编排；实际复制/移动、冲突命名和目标目录校验仍由 `AppExecutionFileSystemAdapter` 负责。

### Bug 收敛

- 同一请求内如果 Finder 传入重复选中路径，传输适配器会按标准化路径去重，避免复制动作生成多个 `xxx 2`、`xxx 3` 类型重复文件。
- 跨通知和队列的重复请求仍由 `AppExecutionRequestHandler` 的 `requestID` 去重机制处理，本次未改变该链路。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionTransferAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.23 / 2026060947`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.24 主 App 剪切动作适配器补充

### 新增适配器

- 类型：`AppExecutionCutAdapter`
- 职责：统一处理剪切动作的 Finder 选中项解析、标准化 URL 去重和剪贴板 move 语义写入。
- 边界：只负责剪切动作业务编排；底层 `NSPasteboard` 类型写入仍由 `AppExecutionClipboardAdapter` 负责，选中项存在性过滤仍由 `AppExecutionFileSystemAdapter` 负责。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionCutAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.24 / 2026060948`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.25 主 App 永久删除适配器边界收敛

### 适配器调整

- 类型：`AppExecutionPermanentDeleteAdapter`
- 职责：在既有二次确认和 `FileManager.removeItem` 基础上，进一步接管 Finder 选中项解析和标准化 URL 去重。
- 边界：请求处理器只负责策略入口；删除目标解析、确认文案、主线程确认弹窗和不可恢复删除执行都在永久删除适配器内完成。

### 安全约束

- 删除前仍强制弹出主线程确认窗口，用户取消时抛出 `userCancelled`，不会删除任何文件。
- 同一请求内重复路径会在适配器内去重，避免重复删除日志和重复操作。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionPermanentDeleteAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.25 / 2026060949`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.26 主 App Finder 信息窗口适配器边界收敛

### 适配器调整

- 类型：`AppExecutionFinderAdapter`
- 职责：统一处理 Finder 信息窗口动作的请求目标解析、标准化 URL 去重、AppleScript `open information window` 调用和脚本失败后的 `NSWorkspace.activateFileViewerSelecting` 回退定位。
- 边界：请求处理器只负责策略入口；Finder 选中项解析、脚本自动化和系统回退行为都在 Finder 适配器内完成。

### 维护约束

- Finder 自动化权限失败时不能中断整个请求，必须回退到 Finder 中定位目标文件。
- 同一请求内重复路径必须在适配器内去重，避免同一文件重复弹窗或重复定位。
- 后续处理系统版本差异、自动化权限诊断或 Finder 回退策略时，优先扩展该适配器，不扩大 `AppExecutionRequestHandler`。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionFinderAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.26 / 2026060950`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。


## V4.0.19 主 App 日志文件适配器补充

### 新增适配器

- 类型：`AppExecutionLogFileAdapter`
- 职责：统一处理主 App 执行日志落盘，包括 App Group 日志路径解析、缓存目录回退、日志文件创建、时间戳格式化和追加写入。
- 边界：只负责日志文件 I/O，不负责日志内容生成、控制台输出、请求策略执行或错误恢复。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionLogFileAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.19 / 2026060943`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。


## V4.0.18 主 App 请求队列适配器补充

### 新增适配器

- 类型：`AppExecutionRequestQueueAdapter`
- 职责：统一处理 Finder Extension 到主 App 的请求队列存储，包括 App Group 队列目录定位、通知 payload 补写、待处理文件排序读取、请求解码和消费清理。
- 边界：只负责队列文件系统 I/O，不负责请求去重、策略选择、动作执行、失败日志和轮询定时器生命周期。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestQueueAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.18 / 2026060942`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。


## V4.0.17 主 App 同名文件夹适配器补充

### 新增适配器

- 类型：`AppExecutionFolderNameAdapter`
- 职责：统一处理按 Finder 选中项名称创建同名文件夹，包括选中项解析、父目录定位、冲突命名和目录创建。
- 边界：只负责“按文件名创建文件夹”业务，不负责策略选择、请求队列、菜单构建或普通新建文件夹动作。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionFolderNameAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.17 / 2026060941`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。


## V4.0.16 主 App 文本工具适配器补充

### 新增适配器

- 类型：`AppExecutionTextToolAdapter`
- 职责：统一处理复制文件名、翻译选中文本和路径二维码等文本派生动作。
- 边界：负责从 Finder 请求解析目标文本，并委托剪贴板、二维码、Web 适配器完成系统交互；不负责策略选择、请求队列或 Finder Extension 菜单构建。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionTextToolAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.16 / 2026060940`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。


## V4.0.15 主 App 可见性适配器补充

### 新增适配器

- 类型：`AppExecutionVisibilityAdapter`
- 职责：统一处理 Finder 隐藏/取消隐藏动作的业务编排、批量目标日志和目录直接子项处理。
- 边界：只负责可见性业务流程，不直接解析 Finder 菜单、不处理请求队列，也不替代文件系统适配器的底层资源值写入。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionVisibilityAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 已安装 `/Applications/右键增强.app`，主 App 和 Finder Extension 均为 `4.0.15 / 2026060939`，插件注册为 `com.maple.right.superright.RightClickFinderExtension(4.0.15)`。
- 最近 5 分钟未发现 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.14 主 App iShot 适配器补充

### 新增适配器

- 类型：`AppExecutionIShotAdapter`
- 职责：统一处理 iShot `.app` 解析、`NSWorkspace.OpenConfiguration` 启动和启动回调日志。
- 边界：只负责 iShot 启动，不负责 Finder 选中项解析、工具箱策略选择、请求队列或日志落盘。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionIShotAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 已安装 `/Applications/右键增强.app`，主 App 和 Finder Extension 均为 `4.0.14 / 2026060938`，插件注册为 `com.maple.right.superright.RightClickFinderExtension(4.0.14)`。
- 最近 5 分钟未发现 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.13 主 App 分享适配器补充

### 新增适配器

- 类型：`AppExecutionSharingAdapter`
- 职责：统一处理 `NSSharingService` AirDrop 分享服务解析、主线程调起和可用性错误。
- 边界：只负责系统分享面板，不负责 Finder 选中项解析、工具箱策略选择、请求队列或日志落盘。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionSharingAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 已安装 `/Applications/右键增强.app`，主 App 和 Finder Extension 均为 `4.0.13 / 2026060937`，插件注册为 `com.maple.right.superright.RightClickFinderExtension(4.0.13)`。
- 最近 5 分钟未发现 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.12 主 App Finder 适配器补充

### 新增适配器

- 类型：`AppExecutionFinderAdapter`
- 职责：统一处理 Finder 信息窗口 AppleScript 自动化和失败后的 Finder 定位回退。
- 边界：只负责 Finder 交互，不负责 Finder 选中项解析、工具箱策略选择、请求队列或日志落盘。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionFinderAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 已安装 `/Applications/右键增强.app`，主 App 和 Finder Extension 均为 `4.0.12 / 2026060936`，插件注册为 `com.maple.right.superright.RightClickFinderExtension(4.0.12)`。
- 最近 5 分钟未发现 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.11 主 App 快捷方式适配器补充

### 新增适配器

- 类型：`AppExecutionShortcutAdapter`
- 职责：统一处理发送快捷方式到桌面的桌面目录解析、冲突命名和符号链接创建。
- 边界：只负责快捷方式落盘，不负责 Finder 选中项解析、工具箱策略选择、请求队列或日志展示。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionShortcutAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 已安装 `/Applications/右键增强.app`，主 App 和 Finder Extension 均为 `4.0.11 / 2026060935`，插件注册为 `com.maple.right.superright.RightClickFinderExtension(4.0.11)`。
- 最近 5 分钟未发现 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.10 主 App 永久删除适配器补充

### 新增适配器

- 类型：`AppExecutionPermanentDeleteAdapter`
- 职责：统一处理彻底删除动作的主线程二次确认、确认文案和不可恢复文件删除。
- 边界：只负责危险删除动作本身，不负责 Finder 选中项解析、工具箱策略选择、请求队列或日志展示。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionPermanentDeleteAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 已安装 `/Applications/右键增强.app`，主 App 和 Finder Extension 均为 `4.0.10 / 2026060934`，插件注册为 `com.maple.right.superright.RightClickFinderExtension(4.0.10)`。
- 最近 5 分钟未发现 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.9 主 App Web 适配器补充

### 新增适配器

- 类型：`AppExecutionWebAdapter`
- 职责：统一处理翻译服务 URL 构造、文本 URL 编码和系统浏览器打开。
- 边界：只负责网页类动作，不负责 Finder 选中项解析、工具箱策略选择、请求队列或日志展示。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionWebAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 已安装 `/Applications/右键增强.app`，主 App 和 Finder Extension 均为 `4.0.9 / 2026060933`，插件注册为 `com.maple.right.superright.RightClickFinderExtension(4.0.9)`。
- 最近 5 分钟未发现 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.8 主 App 二维码适配器补充

### 新增适配器

- 类型：`AppExecutionQRCodeAdapter`
- 职责：统一处理 CoreImage `CIQRCodeGenerator`、二维码纠错等级、缩放倍率和 `NSImage` 输出。
- 边界：只负责二维码图片生成，不负责 Finder 选中项解析、剪贴板写入、工具箱策略选择或请求队列。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionQRCodeAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.8 / 2026060932`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.7 主 App 图片转换适配器补充

### 新增适配器

- 类型：`AppExecutionImageConversionAdapter`
- 职责：统一处理图片到 `.icns`、macOS `.iconset`、iOS `AppIcon.appiconset` 的转换，以及 PNG resize、`iconutil` 调用和 `Contents.json` 生成。
- 边界：只负责图片转换行为，不负责 Finder 选中项解析、工具箱策略选择、请求队列或 UI 交互。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionImageConversionAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.7 / 2026060931`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.6 主 App 剪贴板适配器补充

### 新增适配器

- 类型：`AppExecutionClipboardAdapter`
- 职责：统一处理 `NSPasteboard` 文本写入、Finder 文件 URL 写入与移动语义标记、图片写入。
- 边界：只负责系统剪贴板行为，不负责 Finder 选中项解析、二维码图片生成、策略选择或请求队列。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionClipboardAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.6 / 2026060930`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.5 主 App 图标适配器补充

### 新增适配器

- 类型：`AppExecutionIconAdapter`
- 职责：统一处理 Finder 自定义图标目标解析、用户导入图片解码、内置 SF Symbol 图标渲染，以及自定义图标写入/移除。
- 边界：只负责图标动作本身，不负责请求队列、策略选择、菜单构建、文件复制移动或权限提示。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionIconAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
- 安装验证：`/Applications/右键增强.app` 主 App 与 Finder Extension 版本均为 `4.0.5 / 2026060929`，Finder Extension 已重新注册并启用。
- 崩溃检查：最近 5 分钟无 `右键增强` 或 `RightClickFinderExtension` 新崩溃报告。

## V4.0.4 主 App 文件系统适配器补充

### 新增适配器

- 类型：`AppExecutionFileSystemAdapter`
- 职责：统一处理 Finder 选中项解析、复制/移动目标冲突命名、目录直接子项读取和隐藏标记写入。
- 边界：只负责文件系统行为，不负责 AppKit UI、请求队列、策略选择或日志展示。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionFileSystemAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。

## V4.0.3 主 App 应用适配器补充

### 新增适配器

- 类型：`AppExecutionApplicationAdapter`
- 职责：统一解析外部应用、工具箱应用和 iShot 的真实 `.app` URL。
- 边界：只负责 Bundle ID、默认安装路径、用户配置路径和工具箱别名映射，不负责请求队列、菜单策略或文件操作。

### 影响文件

- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionApplicationAdapter.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强/AppExecutionRequestHandler.swift`
- `/Users/maple/Documents/Project/Swift/右键增强/右键增强.xcodeproj/project.pbxproj`
- `CHANGELOG.md`

### 回归验证

- `swift test`：55 tests, 0 failures。
- `xcodebuild -project 右键增强.xcodeproj -scheme 右键增强 -configuration Debug -destination platform=macOS build`：BUILD SUCCEEDED。
